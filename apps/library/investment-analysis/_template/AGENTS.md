# Legacy Investment template

This template is not used by the lessons linked from the Investment course landing
page. Its slide types, renderer and historical guidance are legacy. For new lessons,
read `../AGENTS.md` and use a linked lesson under `../lessons/`. Edit this directory
only when the user explicitly requests work on the legacy template.
