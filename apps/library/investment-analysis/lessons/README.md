# Current Investment Course HTML lessons

The Investment course landing page identifies the active HTML classroom lessons.
These folders are now their authoritative editable sources:

- `1-1-2-measuring-investment-return/`
- `1-1-3-compound-growth/`

Their shared renderer and local images are stored in `../course-assets/`. The public catalogue intentionally does not link the older `unit-1/lesson-*` routes or the PowerPoint-only Lesson 1.

Create and edit lessons here, within `oehler-huang-platform`. Planning and teaching
preferences are in `authoring/investment-course/` at the repository root. The old
standalone project is a deprecated recovery copy; do not sync from it.

Unlinked lessons and unused lesson types are legacy references. Future lessons
follow the linked decks' renderer, design and demonstrated slide kinds.
